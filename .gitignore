# сборка
/dist/
/awg3panel
/awg3panel.exe

# секреты и локальные артефакты — в репозиторий не попадают никогда
*.conf
!docs/**/*.conf
# фикстуры тестов — ключи в них заведомо фиктивные, они обязаны быть в репо
!**/testdata/*.conf
config.yaml
peers.json
*.key
*.pem

# редакторы
.vscode/
.idea/
